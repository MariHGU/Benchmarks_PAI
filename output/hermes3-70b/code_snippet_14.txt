android {
    compileSdkVersion 33 // Updated compile SDK version

    defaultConfig {
        applicationId "your.application.id"
        minSdkVersion 21 // Minimum required SDK version
        targetSdkVersion 32 // Target SDK version
        versionCode 1
        versionName "1.0"
    }

    // ... rest of the configuration
}